# Ralph Loop Progress Tracker

Task: Optimize AI Coaching Platform spec → master plan + ordered phase files with copy-paste Claude Code prompts.

## Status by iteration

### Iteration 1 (2026-07-15)
- [x] Read original spec, copied to ORIGINAL-SPEC.md
- [x] 3 research agents completed → research/ (github-repos.md, claude-plugins-skills.md, dashboard-ui.md)
- [x] 00-MASTER-PLAN.md written: 20 gaps found & fixed (G1–G20), 11 new features, architecture, sync map
- [x] All 10 phase files written (PHASE-0 … PHASE-9), each with ①learn ②Claude setup ③repos ④pipeline map ⑤prompts ⑥DoD
- [x] README.md index + deferred-decisions list
- [x] **Adversarial fresh-eyes review #1 completed** (independent agent, all 15 files): 3 CRITICAL, 8 MAJOR, 14 MINOR findings
- [x] **All 25 findings fixed**, notably:
  - C1: demo seeder staged (P1 seeds core only; P3/P4/P5/P6 each implement their seed stage)
  - C2: foods table + 150-food verified starter seed created in P2.2 (P3.1 extends); preview kcal computed in code
  - C3: plan_requests full schema (incl. kind diet|split) owned by P2.5; P4/P5 consume
  - M1: new sub-phase 8.6 beta cutover (manual-paid clients → real subscriptions, founder grace for beta trainers)
  - M2: new sub-phase 7.6b monthly progress report + share card
  - M3: splits table defined in P5.2; draft_edits gains entity_type plan|split|reply (P4.3)
  - M4: exemplar embeddings owned by P4.3 nightly job; P6.4 verifies + backfills
  - M5: wearable_daily + manual steps/sleep quick-log created in P3.3
  - M6: custom domains land in P9.5 (Vercel Domains API)
  - M7: all prompt doc references standardized to docs/plan/ (P0.1 copies the plan folder into the repo)
  - M8: pre-plan generic logging mode defined (P3.4) — <24h first-log promise now mechanically true
  - m1–m14: all applied (gate numbering, referral enum, escalation.ts absorption, socials on portal/email/PDF, reminder history mirroring, Stage B captures meal times + weigh-in days, plans_active/splits_active stub schemas, payments checklist step, manual-approve status transition, PROGRESS.md refresh)

## File map
- `ORIGINAL-SPEC.md` — untouched source spec
- `00-MASTER-PLAN.md` — optimized plan: gap analysis → fixes → architecture → phase overview
- `PHASE-0…9` — ordered build phases (PHASE-7 includes 7.6b; PHASE-8 includes 8.6)
- `research/` — July 2026 tooling research (repos, plugins/skills, dashboard UI)

## Verification checklist
- [x] Every phase has learn-first, Claude setup, repos, pipeline map, prompts, DoD + handoff
- [x] Every sub-phase prompt self-contained (docs/plan/ paths resolve after Prompt 0.1)
- [x] Trainer pipeline signup→payout: no gaps (payments checklist step added in 8.1)
- [x] Client pipeline teaser→active→paying: no gaps (pre-plan mode + 8.6 cutover close the loop)
- [x] Cross-phase sync: every table/function created before (or where) first consumed; stubs explicit
- [x] Dashboard phase has dedicated skills/plugins arsenal + design harness
- [x] Research findings incorporated (plugin gaps in P0; dashboard recipe in P7)
- [x] Fresh-eyes re-verification (review #2, independent agent): **all 18 fix-verifications PASS**, zero regressions; 5 residual wording nits (S1–S5) found and fixed same iteration (messages-stub attribution in P3.2, FK wording P3.3→P5.3, wearable_daily schema extension in P9.2, routing-accuracy threshold aligned P6②/6.3, report-note drafts reference in 7.6b)

## FINAL STATE: verified complete (iteration 1)
Two independent fresh-eyes reviews confirm: phases ordered, pipelines in sync (every table/function created before first use, stubs explicit), both onboarding funnels gap-free end-to-end (trainer signup→payout incl. payments checklist step; client teaser→first-log<24h→paying incl. 8.6 beta cutover), all prompts self-contained with resolvable docs/plan/ paths, research incorporated (repos per phase, plugin gaps, dashboard arsenal + design harness).

---

# Build progress (repo: supertrainer)

Sub-phase status for the actual build. Update after each sub-phase's Definition of Done passes.

## Phase 0 — Foundations
- [x] 0.1 Architecture lock-in + repo scaffold (2026-07-15, commit 5464db3) — monorepo boots, typecheck green, placeholder page verified in browser
- [x] 0.2 Supabase project, core schema, RLS (2026-07-15) — 3 migrations apply cleanly, 26 pgTAP RLS tests green (cross-org, cross-client, audit_log isolation, privilege-escalation guards), org_id/user_role JWT claims via custom access token hook, types generated
- [x] 0.3 Auth flows + org bootstrap (2026-07-15) — email OTP + Google OAuth (code path; creds pending), org bootstrap with claim refresh, invites table + RLS tests (33 total green), role-guard middleware, 2 Playwright smoke tests green
- [x] 0.4 Design system baseline + app shells (2026-07-15) — semantic tokens (surface scale, status colors, metric utility) light+dark, TrainerShell (collapsible sidebar + topbar) and PortalShell (bottom tabs) wired as layouts, EmptyState/Skeleton/ErrorBoundary primitives, /styleguide QA route; 3 Playwright checks green (screenshots light+dark & mobile+desktop, zero horizontal overflow, axe-core WCAG AA clean incl. real /portal route), all 5 e2e green
- [x] 0.5 CI/CD + observability (2026-07-15) — GitHub Actions CI (typecheck/lint + local-Supabase RLS/pgTAP + Playwright smoke) and deploy (Vercel prod, `supabase db push` behind a `production` environment approval gate); Sentry (client/server/edge instrumentation + source maps via withSentryConfig, Turbopack post-build hook); PostHog (`track()` in packages/ui + App Router pageview capture + server `trackServer()` dual-writing the events table); Langfuse tracing wrapping getClaudeClient() (model/latency/tokens/cost/task via AsyncLocalStorage). All integrations no-op without keys. Verified: typecheck+lint clean, RLS 39/39, CI env-mapping command, app boots with instrumentation (console clean, guarded /api/debug/sentry → 501). **Interactive setup (repo/secrets/dashboards/branch-protection/PR verification) is documented in docs/ci-cd-observability.md — not runnable in a non-interactive session.**

## Phase 1 — Trainer Onboarding (branch: `phase-1`, COMPLETE 2026-07-16)
- [x] 1.1 Activation checklist engine (2026-07-15, commit e0d3026) — org_onboarding_state table + RLS (staff-only) + grants + 8 pgTAP (47 total green); /onboarding Accordion checklist (progress bar, per-step skip/reopen/deep-link, celebratory completion), events(onboarding_step_completed) via trackServer, dismissible resume banner in TrainerShell; dynamic /onboarding/[step] stub superseded by real routes as sub-phases land. Fixed a latent hydration bug (PostHogPageview useSearchParams → client-bailout Suspense offset React useId, breaking Radix aria ids) by reading window.location in-effect. Verified in browser + 8/8 e2e.
- [x] 1.2 Brand setup + live preview (2026-07-15, commit 6bd4f04) — public-read 'brand' storage bucket (2MB, image types) + org-scoped write policies; /onboarding/brand form (logo upload→Storage, primary color w/ WCAG-AA warning, reserved-word slug + DB-unique, socials), live portal+PDF preview; orgs.brand/slug persisted + step marked done; getOrgTheme(org_id)/BySlug (service-role, public fields) + pure orgThemeVars/brandSocialLinks in packages/ui; /c/[slug] branded landing (P2 teaser stub) + middleware subdomain→/c/{slug} rewrite; PortalShell footer socials. Verified in browser (brand form live preview + /c/{slug}) + 12/12 e2e.
- [x] 1.3 Style-learning ingestion (2026-07-15, commits e2debc3/7b7c1ba + Stage B) — the moat. Schema (uploads/style_profiles/style_exemplars + pgvector) + staff-only RLS + 11 pgTAP (58 total green); private org-scoped 'ingestion' bucket (pdf/docx/image/text). diet/training/voice extraction agents (packages/ai/src/style) via modelRouter('ingest')→Opus 4.8, Zod schemas per §4.2, cached system prompts. Eval harness (`npm run eval:style`) scores **100% field accuracy** on 5 fixtures (gate 90%). Extraction worker (pdf-parse v2 / mammoth / Claude vision, server-external) → upload zone → real drafts → plain-English editable confirmation UX → confirm marks step done. getStyleProfile(org_id,domain) with prompt-cache-friendly serialization (DoD helper). Verified: eval 100%, live e2e (upload→real Opus extraction→confirm) + CI-safe confirmation e2e, browser (upload zone + confirmation UI). 14/14 e2e green. ANTHROPIC_API_KEY now in apps/web/.env.local (also needs CI/Vercel secrets for prod ingestion + eval workflow).
- [x] 1.4 Tier builder (2026-07-15) — tiers table (name/price/currency/cadence/position/features jsonb/stripe_product_id) + staff-only RLS + 7 pgTAP (65 total green); /onboarding/tiers builder pre-filled from the spec §8 template ladder (Basic/Silver/Gold/Platinum), rename/reprice/edit features (checkin frequency, video calls, priority, custom lines), add/remove (max 6, min 1), move up/down reorder; constant AI floor shown as "included in every tier"; reusable branded TierCard in packages/ui (client-facing preview, reused P2 teaser + P8 checkout); saveTiers replaces the set preserving stripe_product_id, marks step done. Verified in browser + 2 e2e (reorder persists across reload; min-1-tier + price validation). 16/16 e2e green.
- [x] 1.5 Client import wizard (2026-07-16) — import_batches table + clients.import_batch_id + staff-only RLS + 6 pgTAP (71 total green); /onboarding/import wizard: CSV (papaparse) / XLSX (sheetjs) parse client-side, live 5-row preview, per-field mapping dropdowns with AI-proposed mapping (mapColumns agent, modelRouter('parse')→Haiku, user confirms — never auto-applies), validation (missing email+phone, invalid email, duplicate email; allergies-unmapped warning), import → lead clients (source=import, intake jsonb, allergies→health_flags) under an undoable batch, post-import checkbox list → Draft invites (unsent invites rows), Undo within 24h. mapColumns/IMPORT_FIELDS kept out of the client bundle (fields.ts self-contained). Verified: 2 e2e (messy CSV: extra cols/unicode/missing+invalid+dup emails → validate → import → draft invites → undo, with real Haiku mapping; allergies-unmapped warning). 18/18 e2e green.
- [x] 1.6 Demo client foundation (2026-07-16) — clients.is_demo + partial index; is_demo added to the client-restricted-columns guard + 3 pgTAP (74 total green). Idempotent staged seeder packages/db/seed/demo-client.ts: seedDemoClient(orgId) finds-or-creates a badged "Alex Demo" (status=active, source=invite, realistic intake + Peanuts allergy), runs stages seedCore now + seedLedger/seedPlans/seedSplit/seedThread registered as no-ops (Phases 3/4/5/6 fill them). Shared excludeDemoClients() query helper in packages/db (every analytics/export/billing count inherits it). /onboarding/demo: create/reset demo, badged DEMO card with intake highlights, "Send yourself the teaser" (org /c/{slug} link + QR + copy). Verified in browser + 2 e2e (create idempotent + badged + step done + teaser; is_demo excluded from aggregates). 20/20 e2e green.
- [x] 1.7 Invites + activation completion (2026-07-16) — invites gains channel (copy_link|email) + personal_message + opened_at; 14-day expiry (74 pgTAP still green). /onboarding/invite: pick an imported lead or enter an email + note, choose copy-link or email delivery, generate a tokenized /join/{token} link (copyable); branded Resend email template (no-ops without RESEND_API_KEY → copy-link fallback); 100 invites/24h/org rate limit. /join/[token]: records the open (invite_opened), trainer-branded welcome, Accept → /join/[token]/accept route handler (full navigation, not a server action, so /auth/confirm sets the session) creates the client's auth account + client-role profile, links the lead's client row, marks the invite used, emits invite_accepted, and lands the client on /portal (P2 Stage B stub). Events: invite_sent/opened/accepted. Verified: 3 e2e incl. the full signup→import→invite→join loop across two browser contexts (client account created, lands on /portal, all three funnel events fire), invalid-token + invalid-email guards. 23/23 e2e green.

**Handoff to Phase 2:** /join/{token} lands a claimed client on /portal (Stage B onboarding route to be built); the branded teaser link is /c/{slug} (P1.2). ANTHROPIC_API_KEY is wired locally; add it (and RESEND_API_KEY, plus the P0.5 secrets) to GitHub Actions + Vercel for CI/prod. All 7 sub-phases verified; 74 pgTAP + 23 Playwright green.

## Security review (branch `fix/security-review`, PR #4 — awaiting merge)
Whole-project review pass over Phases 0–1. 5 fixes: (1) open-redirect via `/\` in safeRelativePath; (2) cross-org invite claim — claimInvite now verifies client.org_id===invite.org_id (service role bypasses RLS); (3) style ingestion confidence from extracted-text count not file count; (4) /api/debug/sentry staff-gated (anon quota abuse); (5) unused `cn` import. Green (typecheck/lint/pgTAP 74/Playwright 23). **NOTE:** phase-2 was branched from main BEFORE #4 merged, so it still carries the pre-fix versions of routes.ts/claim.ts/sentry route/style actions/tier-builder — merge #4, then merge main into phase-2 (the `cn` lint warning on phase-2 clears then).

## Phase 2 — Client Onboarding, Teaser Funnel & Consent (branch: `phase-2`, IN PROGRESS)
- [x] 2.1 Stage A teaser intake (2026-07-16) — `leads` table (email/phone/answers jsonb/allergens text[] NOT NULL/status/turnstile_verified/converted_client_id) + lead_status enum; RLS read-only for org staff, NO insert grant for API roles (public writes go through the submit action's service role) + 7 pgTAP (81 total green). Public one-question-per-screen flow at `/c/{slug}/start` (org-branded via getOrgThemeBySlug): 13 steps (name/email/phone/age/sex/height/weight/goal/activity/training-days/experience/diet/allergies), progress dots, back support, per-step validation mirroring the shared Zod schema (stage-a.ts — single source of truth for form + server). Allergies require an EXPLICIT choice (list ≥1 OR "I have no allergies") — never a silent default (superRefine + client gate). Cloudflare Turnstile bot-gate (turnstile.ts, no-ops without keys per project convention; verified server-side before persist; fail-closed on network error). Postgres sliding-window rate limits on leads.created_at: 3/email/week + 50/day/org. Fires `lead_created`. `/c/{slug}` landing CTA now links to /start. env.example documents NEXT_PUBLIC_TURNSTILE_SITE_KEY + TURNSTILE_SECRET_KEY. Verified: typecheck 4/4, lint clean (only PR#4's `cn`), 81 pgTAP, 32 Playwright (9 new: full flow + column/answers split + event, explicit-none gate, per-org daily limit, per-email weekly limit, 5 Turnstile decision-logic branches), browser (branded mobile form renders, 0 console errors).
- [x] 2.2 Blurred preview generator (2026-07-16) — the conversion mechanic. **foods table** (v0 verified-nutrition DB: source enum usda|off|ifct|org_custom|seed, name_normalized, cuisine/allergen_tags, serving_units, per-100g macros, verified; nullable org_id = global vs org_custom) + `food_source` enum + GIN allergen index + partial global-unique key; RLS reads global + own-org foods, writes service-role only (+7 pgTAP). Seed: **128 foods** across cuisines incl. Indian staples (packages/db/seed/preview-foods-seed.json, per-row USDA/IFCT citations; migration seed generated from the JSON so they can't drift). **packages/ai/allergens.ts** — deterministic canonical taxonomy (peanut/tree_nut/dairy/egg/soy/gluten/fish/shellfish/sesame/coconut) + `filterSafeFoods`, fail-closed (tag map + free-text name-net; over-excludes on ambiguity). Preview agent (packages/ai/preview.ts, modelRouter('draft')→Sonnet, trainer voice injected, Zod-validated) SELECTS foods by id from an already-filtered pool + prescribes sets/reps — never emits numbers. generate.ts: allergen filter → diet filter (veg/non_veg/vegan) → agent → re-validate every returned foodId ∈ pool (drops hallucinations) → **macros computed in code** (macros.ts, rule 4) → cached on leads.preview (never regenerates). Blurred-remainder preview UI at /c/{slug}/preview/{leadId} (real rows blurred, not truncated) + branded TierCard unlock. Conversion route /c/{slug}/preview/{leadId}/convert (full-nav, mirrors invite-accept): creates client (status=onboarding, source=teaser, linked via leads.converted_client_id, allergens→health_flags, selected tier in intake) + auth account + magic-link → /portal; fires lead_converted. clients.approved_manually column + restricted-columns guard extended (client can't self-approve, +3 pgTAP). leads gains preview/preview_generated_at. Verified: typecheck 4/4, lint clean, **91 pgTAP**, **39 Playwright** (+7: allergen property-test across the seed + tricky cases, preview render/cache, conversion creates correct rows, live generation is allergen-safe for a veg dairy/peanut-allergic lead), browser (branded preview renders, computed kcal, blur effect, 0 errors). Security review of the public endpoints: tenancy verified in code, no unbounded paid-AI (cached + unguessable leadId), model text rendered as escaped React children, allergen guarantee double-checked (filter-before + validate-after).
- [x] 2.3 Consent gate (2026-07-16) — blocking click-wrap consent before any portal route. `consents` table (org_id/client_id/doc_version/doc_sha256/signed_name/signed_at/ip/user_agent) append-only (SELECT-only for API roles: staff read own org, client reads own; writes service-role) + private `consents` storage bucket (staff-read) + 6 pgTAP (97 total green). Versioned legal template `/legal/consent-v1.md` with a **Lawyer TODO** block (jurisdiction/liability/health/minors/arbitration) stripped before display+hash; bundled to apps/web/lib/consent/template.ts (generated, no runtime fs). doc.ts renders the canonical text (placeholders filled, comment stripped) + stable sha256; **the exact on-screen text is what's hashed**. /consent page (self-guards non-clients + already-signed) + ConsentForm: scrollable doc, **scroll-to-bottom + typed name + checkbox** all required to enable signing. recordConsent action captures ip/UA/hash/timestamp server-side, inserts the consent, sets clients.consent_signed_at/consent_doc_hash (service — restricted columns), and generates a branded **react-pdf** copy → private bucket + Resend email (best-effort, no-op without key). Guard in the portal layout (wraps all /portal/*) redirects un-consented clients to /consent; +middleware /consent auth rule. Invite/teaser-claimed clients now correctly land on the consent gate first. Verified: typecheck 4/4, lint clean, 97 pgTAP, **41 Playwright** (+2: hash stability + context-sensitivity, and the full gate→scroll→sign→portal flow with evidence-trail assertions incl. the real-runtime PDF `%PDF` stored to the bucket). Existing portal/role tests updated (seed consent) — the funnel-landing test now asserts the gate. **Lawyer review of consent-v1.md required before production.**
- [x] 2.4 PWA install + notification walkthrough (2026-07-16) — the adherence engine's delivery dependency. `push_subscriptions` (org_id/client_id/endpoint unique/keys jsonb/platform/revoked_at; multi-device) + RLS (client manages own, staff read org, **no DELETE grant** — soft-revoke only) + `notification_channel` enum push|email_only on clients (defaults **email_only**; only moves up on real permission) + 7 pgTAP (104 total green). Installable PWA: **dynamic org-branded manifest** at /manifest.webmanifest (auth-aware — a client installs their COACH's app; `crossOrigin="use-credentials"` on the link or the browser gets the generic one) + `/api/icon` generating branded PNGs via next/og (trainer logo, letter-avatar fallback on brand color w/ WCAG-readable text); minimal service worker (`public/sw.js`, navigation-only network-first → cached `/offline` shell; deliberately never caches API/auth/page responses) + registration. `/welcome/notifications` walkthrough, platform-detected (pure `detectPlatform`/`isStandaloneFrom` — iPadOS-reports-as-Macintosh handled via touch points): **iOS** → illustrated Share→Add-to-Home-Screen (push is impossible until standalone) w/ standalone return-detection; **Android** → beforeinstallprompt captured → custom install button → permission; **desktop** → straight to permission. "This is how {trainer} reaches you" + sample coach notification. Skippable by design → records email_only and shows what they'll miss. Events: push_enabled/push_skipped/pwa_installed. Consent now hands off here (skippable, so the portal stays reachable). VAPID keys documented in .env.example (no-op without → channel still recorded, P6 falls back to digest). Verified: typecheck 4/4, lint clean, 104 pgTAP, **47 Playwright** (+6: platform/standalone detection, org-branded vs anon manifest + icon renders as real PNG, and all three permission outcomes granted/denied/skipped asserting channel + events).
- [x] 2.5 Stage B conversational interview (2026-07-16) — `messages` STUB (org/client/sender enum/kind/body/payload; P6.1 extends with realtime+receipts+full kind enum — interview turns persist here so history carries into the real thread), `interview_state` (section/answers/status/last_prompt_at/nudges_sent/started_at), `plan_requests` FULL schema (kind diet|split, trigger onboarding|monthly|manual, status queued|running|drafted|failed — P4/P5 consume) + RLS (messages/interview_state read-only for API roles: staff read org, client reads own, **all turns service-role written**; plan_requests staff-only queue clients never see) + 9 pgTAP (**113 total green**). **packages/ai/escalation.ts** — fail-closed health gate v1: deterministic word-boundary keyword pass (condition/medication/pregnancy/injury/eating_disorder) ∪ Haiku classifier told to flag-when-unsure; a keyword hit can never be cleared by the classifier and a classifier outage degrades to keywords, never to "safe". **packages/ai/interview.ts** — one-question-per-turn agent (modelRouter('draft')→Sonnet, trainer voice injected), per-section Zod schemas, parses free-text → typed fields, confirms understanding, adaptive follow-ups; **code (not the model) decides section completion** via isSectionComplete; day 1–3 pacing (logistics+goals / nutrition+training / lifestyle+health) + resumable. Engine: health screen runs BEFORE the coaching agent → disclosure writes clients.health_flags (**merged — teaser allergens preserved**), pauses to `paused_health`, replies "your coach will follow up personally", fires health_flag_raised (P7's review-queue signal). Completion → intake assembled onto clients.intake (teaser intake preserved under stage_b) → profiles.timezone/locale propagated (P3 reads these) → `intake_complete` → one diet + one split plan_request queued. Chat UI at /welcome/interview (+ portal "Finish your intake" CTA); pure isNudgeDue (24h, max 2) for P6. **Eval: `npm run eval:interview` — 6/6 personas pass** (straightforward, **Hinglish**→Asia/Kolkata, shift-worker, diabetes+metformin→flagged [condition,medication] via both, pregnancy→flagged, **joker→captured nothing rather than fabricating**). Verified: typecheck 4/4, lint clean, 113 pgTAP, **53 Playwright** (+6: keyword gate incl. no-false-positives on soreness & word-boundary pillow/hearty, P3 must-capture completion, day pacing, nudge cap, health-disclosure pause w/ allergen-merge + event, live completion → intake + timezone + 2 queued plan_requests + event).

**Phase 2 — COMPLETE (2026-07-16).** Teaser → preview → conversion → consent → install/notifications → Stage B interview → intake complete → plan_requests queued. 113 pgTAP + 53 Playwright green; interview eval 6/6; allergen property-test across the whole foods seed.

### Phase 2 multi-agent review + fixes (2026-07-16, on branch phase-2)
Ran a dynamic 6-angle review (Phase-1 regression / correctness / security / data-sync / DoD-gaps / optimization) with adversarial verification. 42 findings → **15 applied**, verified by 3 parallel regression agents (Phase-1 SAFE, both Phase-2 safety invariants intact, data IN SYNC):
- 🔒 **convert.ts** reworked — account created BEFORE the client (email uniqueness is the concurrency guard → no duplicate client, no orphan on email-taken); an already-converted lead now goes to /login instead of re-minting a session (a leaked preview URL is not a standing credential); health_flags key aligned to `allergies` (matches P1 import/demo).
- 🔒 **/api/icon SSRF** closed — only fetches logos from the Supabase Storage public prefix (was: any attacker URL).
- 🔒 **enablePush** validates the endpoint is HTTPS + a known push host before storing (was: unchecked stored-SSRF target for P6's worker).
- **Consent gate extended to /welcome/*** — an un-consented client can no longer run the Stage B interview (new requireConsentedClient helper).
- **Branded-subdomain funnel fixed** — middleware now rewrites only the root `/` to /c/{slug}; every real app route (portal/consent/welcome/offline/PWA) passes through (was: whole funnel 404'd + SW install failed on branded subdomains). Removed dead isBrandedPassthroughPath.
- **Interview engine** — client message now recorded only after a successful turn (no orphaned/duplicated message on AI failure, no double-show); completeIntake assembles intake + queues plan_requests + is idempotent BEFORE marking status='complete' (a mid-way failure can't strand a "complete" interview with no intake); self-heal on next visit.
- **generate.ts** — preview cached unconditionally (converted leads no longer re-pay Sonnet), fires `preview_shown`, dedups foodIds (no double-counted macros / dup React keys).
- **escalation.ts** — hyphenated keywords (self-harm, beta-blocker) now fire on the keyword floor (separator normalization; only ever adds matches).
- **Empty "training days"** no longer silently recorded as 0 (client + server reject empty).
- Plus: mobile-viewport on all client-funnel specs (DoD), consent name length cap, pwa_installed fires once per device, a foods `where org_id is null` partial index.
- **Deferred (noted for later phases):** per-org DoS sublimit + email-normalization (design changes to the spec's Postgres limiter), stall-condition wiring + push-storage e2e (need P6/P7 + VAPID keys), plan_requests partial-unique DB backstop (count-check suffices for the single-message chat flow), a few sub-ms duplicate-query micro-opts.

Re-verified after fixes: typecheck 4/4 · lint clean · **113 pgTAP** · **53 Playwright** (incl. live allergen + live interview completion).
**Handoff to Phase 3:** intake complete + timezone known (profiles.timezone) + notification channel known (clients.notification_channel) = the ledger can start prompting. `plan_requests` rows sit `queued` for Phase 4/5. Stage B must-captures live at `clients.intake.stage_b` (nutrition.mealsPerDay + mealTimes drive reminder defaults; logistics.weighInDays drives weigh-in prompts). **Open items:** `legal/consent-v1.md` needs lawyer review before production; Turnstile + VAPID + Resend keys needed in prod (all no-op without); P7 must surface `clients.health_flags.interview` + `paused_health` interviews in the review queue (a paused interview currently waits on the trainer with no UI yet).

## Phase 2 deferred backstops (branch `phase-2-backstops`, 2026-07-18)
Built the three hardening items deferred at the Phase 2 review (spec: `docs/superpowers/specs/2026-07-18-phase2-deferred-backstops-design.md`; plan + SDD ledger under `docs/superpowers/plans/` + `.superpowers/sdd/`):
1. **Stage A rate-limiter hardening** — `leads.email_normalized` (count-only key collapsing case/`+tag`/Gmail-dot variants) + `leads.ip_hash` (HMAC of client IP, never the raw address); new pure `apps/web/lib/onboarding/rate-limit.ts` (`normalizeEmail`/`hashIp`/`rateLimitDecision`); `submitLead` counts the weekly limit on the normalized email and adds a per-source/day DoS sublimit (`PER_IP_DAILY_LIMIT=5`, keyed on `ip_hash`, skipped when `LEAD_IP_HASH_SECRET` unset).
2. **Delivery-agnostic stall-nudge cron** — `apps/web/lib/interview/stall.ts` `runInterviewNudges()` nudges `in_progress` interviews idle >24h that still have an open section (never the between-days wait), CAS-leasing each row on `last_prompt_at` before posting an in-thread nudge + `interview_nudge_sent`; once-daily `GET /api/cron/interview-nudges` (Vercel Hobby caps crons at once/day; fine for a 24h-idle nudge; fails closed on `CRON_SECRET`); `notifyClient()` is the Phase-6 push/email seam. `dayNumber` lifted to `apps/web/lib/interview/pacing.ts`.
3. **`plan_requests` onboarding partial-unique index** `(client_id, kind) WHERE trigger='onboarding'`; `completeIntake` now treats a `23505` as already-queued (no duplicate rows, no double `intake_complete`).

Verified: typecheck 4/4 · lint clean · **pgTAP 118** · **Playwright 57/57 non-AI green**. The 3 live-AI e2e tests (interview/preview/style) are blocked ONLY by the Anthropic account being out of API credits (proven via direct API probe — `HTTP 400 credit balance too low`), not by any code change. New env vars `LEAD_IP_HASH_SECRET` + `CRON_SECRET` documented in `.env.example` (both no-op-safe; cron fails closed).

**Still deferred (unchanged intent):** nudge push/email *delivery* + push-storage e2e (Phase 6 + VAPID keys); phone *verification* (SMS/OTP) + phone-uniqueness-at-conversion (later phase — low value until the phone is verified, and family-shared numbers); the few sub-ms duplicate-query micro-opts. **Ops before prod:** top up Anthropic credits for the live-AI tests; `vercel.json` lives at `apps/web/vercel.json` (Vercel project Root Directory = `apps/web`); set `CRON_SECRET` (and optionally `LEAD_IP_HASH_SECRET`) in the Vercel/CI env.

## Pre–Phase 3 hardening audit — SHIPPED (2026-07-21, PR #7, commit `1e63677`)
Deep multi-agent audit of Phases 0–2 + backstops (adversarially verified; report `docs/audit/2026-07-21-pre-phase3-audit.md`) → all **14 defects fixed** (8 MUST-FIX + 6 SHOULD-FIX), plus a security-review follow-up (health classifier always runs under throttle) and a max-effort-review fix (opener lease made self-invalidating). Verified typecheck 4/4 · lint · pgTAP 120 · Playwright 75; merged to main; migrations `demo_client_unique` + `lead_preview_lock` applied to prod. Highlights: MF-1 allergen name-net now enforces multi-word pick-list labels (pea/dal safe); MF-4/MF-7 partial-unique + claim-lock backstops; MF-5 consent IP via trusted hop; MF-6/8 interview rate-limit + consent gate.

**This is the AUDIT BASELINE** (`docs/audit/AUDIT-BASELINE.md`): future hardening audits scope ONLY to new-phase diffs vs `1e63677` — Phases 0–2 are NOT re-audited.

## Audit PROPOSE-ONLY features (PO-1…PO-6) — SHIPPED (2026-07-22, PR #8, commit `8748b59`)
Built the 6 PROPOSE-ONLY features from the pre-Phase-3 audit (spec `docs/audit/apply-po-features.md`), one commit each off baseline `1e63677`:
- **PO-3** consent re-sign on material doc-version bump (code version registry; `clients.consent_doc_version` backfilled + guarded; v1 dormant, zero current clients re-sign).
- **PO-5** auto client brief on Stage B completion (`clients.brief`; neutral-voice draft, health flags code-derived, PII dropped from the prompt; best-effort claim-guarded).
- **PO-6** lead-intent scoring on teaser submit (`leads.intent_band` enum + `intent_reason`; qualitative only; best-effort).
- **PO-1** trainer `/trainer/prospects` pipeline (reuses leads RLS; "convert manually" = CAS-guarded inline invite).
- **PO-2** style-strength/coverage meter (`lib/style/coverage.ts`, code-computed; additive re-extraction, corpus-capped).
- **PO-4** AI resilience in modelRouter/zodOutput (`packages/ai/resilience.ts`: retry/backoff + Sonnet→Haiku fallback + per-model circuit breakers + `isAiDegraded()` funnel flag).

3 new migrations (`consent_doc_version`, `client_brief`, `lead_intent`). Reviewed in 3 passes — max-effort `/code-review` (12 fixes) + `/security-review` (clean) + a second local max-effort pass (4 more fixes, incl. a circuit-breaker probe-leak) — all fixed. Green: typecheck 4/4 · lint · **pgTAP 124** · **Playwright 106**; PR #8 CI green; squash-merged to main. **This is the new AUDIT BASELINE** (`docs/audit/AUDIT-BASELINE.md`, `8748b59`).

## Phase 3 — Adherence Ledger & Logging Pipelines (branch: `phase-3`, IN PROGRESS)
- [x] 3.1 Verified nutrition database (2026-07-22) — the macro-math foundation; extends the P2.2 foods table (128 → **215 global foods**), no LLM arithmetic anywhere. **Search infra** (`20260722140000_food_search.sql`): `pg_trgm` (schema-qualified per the hosted-extension gotcha), trigram + `simple` FTS indexes on foods, and `search_foods(query, locale, org, limit)` — one indexed round-trip ranking exact > prefix/alias > full-text > trigram fuzzy, org-scoped for both RLS (authenticated) and explicit `p_org` (service-role) callers, locale as tiebreaker only. **`food_aliases`** table (regional/colloquial names → a food; 55 seeded, e.g. `chawal`→rice, `chai`→masala chai) with RLS mirroring the parent food's visibility + org-staff writes limited to their own custom foods' aliases. **org-custom write path** (`20260722140100_food_org_custom.sql`): `foods_org_custom_ownership` CHECK (org_custom ⇔ org-owned) + insert/update/delete policies scoping trainers to their OWN org_custom foods; `createOrgCustomFood`/`buildOrgCustomFood` in packages/db require an explicit allergen decision and UNION the fail-closed derivation on top. **Indian seed** (`20260722140200_indian_foods.sql`, GENERATED from `packages/db/seed/indian-foods-seed.json` + `food-aliases-seed.json` via `scripts/gen-foods-migration.ts` so rows can't drift): 87 net-new cited staples (16 verified single-ingredient; 71 recipe estimates flagged `verified=false`), deduped vs the existing ~40 Indian foods. **Allergen tagger** (`src/allergens.ts`, shared by the generator + runtime): deterministic name/ingredient → tag mapping, word-boundary matched (fixed the `len[til]`→sesame / `[egg]plant`→egg traps) and free of the bare-`nut`/`roti` traps; declared ∪ derived, fail-closed. **`searchFoods()` + `resolveGrams()`** in packages/db (`"2 rotis"`→80 g via serving_units/name-token match; mass + household units). **USDA FDC importer** (`scripts/import-usda.ts`, Foundation + SR Legacy, branded skipped): idempotent on the global natural key, resumable via a checkpoint, allergen-tagged on import — verified against a committed sample bundle. Verified: `supabase db reset` clean · **pgTAP 149** (+25: foods org_custom writes, food_aliases RLS, search_foods ranking/org-scope) · `npm run verify:nutrition` all pass (10-food spot-check, `2 rotis`=80 g=238 kcal, alias resolution, USDA import twice = no dupes) · typecheck/lint clean · P2.2 allergen property test green across all 215 foods. Commit: "feat(nutrition): verified food database with allergen tags".
- [x] 3.2 Meal logging (text, photo, voice) (2026-07-22) — the 10-second logging loop; verified-DB numbers only (the model splits text/photo into `{name, qty, unit}`; all macros computed in code). **Tables** (`20260722150000_meal_logs.sql`): `meal_logs` (items/totals jsonb, `tz_date` bucketed by the client's timezone, method text|photo|voice, photo_path) + the **`plans_active` stub** (P4.3 fills it; confirm card compares to targets when present, generic mode otherwise) + **`meal-photos`** private bucket ({org}/{client} paths). RLS: client reads own, staff read org, writes service-role. **Agents** (`packages/ai/meal.ts`): `parseMealText` (modelRouter 'parse'→Haiku, Zod) and `proposeMealFromPhoto` (vision, same shape) → one shared pipeline; **`voice.ts`** provider-agnostic STT seam (no-op-safe → UI degrades to typing). **Pipeline** (`lib/ledger/resolve.ts`): parse → `searchFoods` → best match + top-3 + portion (`resolveGrams`) → `macrosFor`; `computeConfirmedItems` recomputes authoritatively from the DB, ignoring client numbers; unknown foods logged unverified (null macros, trainer-flagged). **Write** (`lib/ledger/log.ts`): service-role, session-derived client (tenancy in code), inserts the log + mirrors a `log_confirmation` system message (P2.5 thread) + fires `meal_logged`. **Portal** `/portal/log`: slot auto-guessed from local time, text input + photo + voice, confirm card with per-item picker + portion stepper (≤2 taps: Add → Confirm). Verified: `supabase db reset` clean · **pgTAP 159** (+10: meal_logs/plans_active client-own vs staff-org reads, cross-org isolation, no client writes) · typecheck/lint clean · **e2e**: pipeline (2 rotis=80 g=238 kcal, unverified handling, authoritative recompute) + mobile UI render, both CI-safe; the live ≤2-tap flow guarded on ANTHROPIC_API_KEY. Commit: "feat(ledger): meal logging text/photo/voice with verified-DB confirm step".
- [x] 3.3 Weigh-ins, check-ins, working sets, photos (2026-07-22) — the remaining logging surfaces, all offline-tolerant. **6 tables** (`20260722160000_logging_surfaces.sql`): `weigh_ins` (kg-normalized), `gym_checkins`, `workout_logs` (per-set; exercise_id free text until P5.3's FK), the **`splits_active` stub** (P5.3 fills it), `progress_photos` (front/side/back + private `progress-photos` bucket), `wearable_daily` (manual steps/sleep; P9.2 auto-fills) — each with a NATURAL KEY the writes upsert on, RLS (client-own / staff-org, service-role writes). **Offline** (`lib/offline/queue.ts`): IndexedDB queue → replay on `online`; correctness rests on the idempotent upserts (a replayed write is a no-op, never a dupe). **Auto-satisfy** (`lib/ledger/checkin.ts`, pure/tested): logging any working set forces the day's check-in to `trained` and a later "rest" tap can't undo it. **Surfaces**: portal-home quick-log (weigh-in kg/lb, one-tap check-in, 10-second steps/sleep), `/portal/workout` (pre-filled from the split, previous-session ghosting + "same as last time", freeform add), `/portal/progress` (front/side/back). Verified: `supabase db reset` clean · **pgTAP 170** (+11: all 6 surfaces' client-own vs staff-org reads, cross-org isolation, write-denial) · typecheck/lint · **e2e**: pure rules (auto-satisfy, kg/lb) + idempotent-replay + auto-satisfy through the real write path + mobile UI render, all CI-safe; **portal axe AA clean** in light+dark (fixed `<button>`/input dark-mode contrast, incl. a latent one in the 3.2 meal logger). Commit: "feat(ledger): weigh-ins, check-ins, working sets, photos".
- [x] 3.4 Day-close & auto-miss engine (2026-07-22) — TDD-first (fixture suite written before the engine). **Pure engine** (`lib/ledger/day-close.ts`, dependency-free): `computeExpectations` (paused/onboarding/churned → none; active+no-plan → generic ≥2 meals + intake weigh-in days; active+plan → plan slots + training-day sets/check-in) → `computeMisses` (expected-but-absent, check-in auto-satisfied by sets) → `evaluateDay`; `dayHasEnded` is DST/travel-correct via Intl. **`ledger_days`** table (expected/actual/misses jsonb, `late`, `closed_at`; one row per client-day) + RLS. **Close job** (`lib/ledger/day-close-job.ts`): gathers a finished day's real logs, evaluates, upserts an auto-miss row (never blank), idempotent (skips closed days), events injected (no server-only dep → unit-testable); `recomputeDay` reopens a day as `late` when a back-dated log lands. **Cron** `/api/cron/day-close` (CRON_SECRET, fails closed) + daily Vercel cron (window=2 catches missed runs; 15-min Supabase pg_cron documented as the prod ideal). Verified: `supabase db reset` clean · **pgTAP 176** (+6: ledger_days client-own/staff-org reads, cross-org, write-denial) · typecheck/lint · **e2e**: **35 pure fixtures** (generic/plan, partial-log permutations, rest days, paused/onboarding/churned, late, DST Toronto spring-forward + Kolkata + traveler) + **4 integration** (auto-miss, idempotent, paused, late-recompute), all CI-safe. Commit: "feat(ledger): timezone-correct day-close and auto-miss engine".
- [x] 3.5 Two-lens scoring, streaks, macro banking (2026-07-22) — TDD-first (fixture suite before the engine). **New `packages/scoring`** (pure, dependency-free): `weeklyAdherenceScore` (weighted meals 40 / weigh-ins 15 / training 30 / check-ins 15, averaged over the week + normalized over applied components so **one missed meal is never a cliff**; no-expectations → 100); `scoreBand` (<50 reset / 50-75 building / >75 locked-in) with a **shame-free copy bank** (asserted against a banned-word list); `streakCount` + `comebackStreakDisplay` (exact `floor(previous/2)+comeback`); macro banking (`bankDay`/`runningBuffer`/`bufferAfterDay`, cap 150, floors at 0, weekly Monday reset by the caller); trainer-lens pattern flags (`weekendFaller`, `weighInAvoider`, `loggingDecaySlope` least-squares). **Adapter** (`lib/ledger/score.ts`): `dayScoreFromLedger` turns a stored `ledger_days` row into per-component satisfaction; `computeClientLens` rolls up score + band + streak. **Client lens**: `ClientScoreCard` on the portal home shows the weekly score (band-coloured), streak, and supportive copy — the score IS shown to clients (§5). **Typed series** `ledgerDaysInRange` in packages/db for P7 charts. Verified: typecheck 5/5 · lint · **e2e**: **~50 pure-math assertions** (score sensitivity, bands + no-shame copy, streak/comeback, banking edge cases incl. over-cap & negative, pattern flags) + adapter tests + a client-lens card render, all CI-safe. **Deferred (blocked on P4 targets):** the macro-banking confirm-card line + trainer-set org toggle — the arithmetic is built and tested; it activates once `plans_active.targets` exists (P4). Trainer-lens grid/trend UI is P7 (data + flags provided here). Commit: "feat(scoring): two-lens scores, streaks, comeback, macro banking".
- [x] 3.6 Reminder engine (2026-07-22) — TDD-first decision rules (the spam-risk core). **Decision engine** (`lib/reminders/decide.ts`, pure): quiet-hours deferral (wrap-around window, inclusive start/exclusive end), 3/day cap by priority (meals > weigh-in > check-in > custom, counting already-sent), already-logged suppression, vacation/paused kill switch — 12 fixtures. **Tables** (`20260722180000_reminders.sql`): `reminder_rules` (schedule/quiet_hours/enabled, one per client-kind) + `notifications` queue (channel push|email|in_app, status, `dedupe_key` unique) + RLS (client-own/staff-org, service-role writes). **Tick** (`lib/reminders/tick.ts`): day-aware (weigh-in days), seeds defaults for fresh clients (`rules.ts`, from intake meal times / weigh-in days), enqueues surviving reminders (idempotent via dedupe_key) with the fallback-ladder channel (push else email), and **mirrors each into the thread** (`messages` kind='reminder', sender='system') for scrollable prompt history (§10) — 5 integration tests (enqueue+mirror, suppression, quiet-hours defer, idempotent, vacation). **Copy bank** (`copy.ts`): personal-feeling, coach-named, 140-char-capped templates (AI personalization seam noted). **Kill switch**: `setReminderVacation` + `setReminderVacationAction` + a portal toggle. **Cron** `/api/cron/reminders` (CRON_SECRET, fails closed) + daily Vercel cron (5-min Supabase pg_cron documented as the prod cadence). Verified: `supabase db reset` clean · **pgTAP 183** (+7: reminder_rules/notifications client-own/staff-org, cross-org, write-denial) · typecheck 5/5 · lint · **e2e 17** (12 decision + 5 tick) + portal axe AA clean. Commit: "feat(reminders): timezone-correct reminder engine with caps".

## Phase 3 — Adherence Ledger: **COMPLETE** (2026-07-22, branch `phase-3`, PR #10)
All six sub-phases shipped and CI-green: verified nutrition DB → meal/photo/voice logging → weigh-ins/check-ins/working-sets/photos → timezone-correct day-close & auto-miss → two-lens scoring/streaks/comeback/banking → reminder engine. The whole client-logging + adherence layer is built. **pgTAP 124 → 183; new packages/scoring; ~14 new tables.**

**SHIPPED (2026-07-23, PR #10, merge `2baacaf`).** Reviewed via a max-effort code-review (all findings fixed + re-verified: an allergen-tagging fail-open, a photo-bucket tenancy hole, two feature-breaking reminder bugs, + mediums). Squash-merged to main; the 7 new migrations applied to prod via "Migrate (production DB)". AUDIT-BASELINE advanced `8748b59 → 2baacaf`. **Ops for prod:** the two new cron routes (`/api/cron/day-close`, `/api/cron/reminders`) reuse `CRON_SECRET` and fail closed without it; the 5-min pg_cron cadence for day-close/reminders is documented as the prod-ideal (Vercel Hobby runs them daily). Dormant until later phases: macro-banking display + org toggle (P4 targets), vision/STT live paths (credits/provider), trainer-lens grid (P7).

**Next build phase: Phase 4 — Diet Plan Generator** (`docs/plan/PHASE-4-diet-generator.md`) — fills the `plans_active`/`plans` stubs, activates the dormant macro-banking display + targets, and consumes the ledger series for monthly adjustments.

## Phase 4 — Diet Plan Generator (branch: `phase-4`, IN PROGRESS)
- [x] 4.2 The multi-agent pipeline (2026-07-23) — structure → recipe → **coded validate + fit + retry** → review, with typed handoffs and **injectable agents** (so the merge gate drives the real control flow deterministically; production passes the live LLM agents). **Agents** (`packages/ai/src/diet-pipeline/`, all via `zodOutput(task:"plan"/"draft")`, style block prompt-cached via `serializeStyleProfile`): `structureAgent` (meal skeleton per day type in the trainer's structure), `recipeAgent` (fills from the injected allergen+diet-filtered pool ONLY, food_id+grams, 2 distinct versions, prep notes), `reviewAgent` (style-match/practicality/variety critique). **Coded core** (`packages/nutrition-engine`, no LLM arithmetic): `validatePlanVersion` recomputes every macro from the food rows and asserts §④ (|Δ|≤3% kcal & ≤5g protein, every id in the safe pool, **zero excluded-allergen tags**) with structured retry feedback; `fitPortions` nails the grams on the model's food choices (scale-to-kcal, then a kcal-neutral protein correction — the model selects, code computes); `assembleMeals` deterministic filler. **Orchestrator** (`generateDietPlan`): validate → one recipe retry with the exact gaps → **per-day-type deterministic fallback so the trainer always gets a valid draft** (needs_attention reserved for a pool that genuinely can't meet targets — a deliberate improvement over blocking), one Langfuse trace per plan (`withPlanTrace`, cost auto-derived, no-op without keys). **DB orchestrator** (`apps/web/lib/plans/run.ts` `runDietPipeline`, client-injected + org-checked in code): reads the queued `plan_request`, builds the safe pool (`buildSafePool` = `filterSafeFoods`→`filterByDiet`), runs the pipeline, writes a draft `plans` row (needs_attention as a content flag — the enum has no such value) + advances the request. **Golden fixtures** (`GOLDEN_INTAKES`, 12: both sexes, cut/bulk/recomp, vegan+nut-allergy, Indian, IF, carb-cycle, 1200-kcal small-female). CI merge gate uses a deterministic filler; live `npm run eval:diet` exercises the real agents (100% draft + 100% allergen-safe verified locally on the hard cases). Verified: typecheck 6/6 · lint 0 errors · **Playwright +31 diet specs** (validator/fitPortions/assembleMeals math, orchestrator control-flow incl. sanitize/retry/needs_attention/allergen-backstop, **12-fixture full-taxonomy allergen property test**, runDietPipeline DB path — all CI-safe, no model) · **live eval green**. No migration (pgTAP stays 191). Commit: "feat(diet-pipeline): multi-agent generation with coded validation".
- [x] 4.1 Plan schema + calculation module (2026-07-23) — the coded math foundation, TDD-first (25 fixtures written before the engine). **New `packages/nutrition-engine`** (pure, zero AI imports, zero DB access; mirrors `packages/scoring` — `type:module`, single root export, `typecheck`-only): `mifflinStJeorBMR` (male +5 / female −161 / other·prefer_not averaged to −78 + `sex_estimated` flag), `activityFactor` (job-type base 1.2–1.9 **+ 0.05/training-day**, bounded, trainer-overridable), `tdee`; `calculateTargets(intake, styleDefaults?, override?)` → kcal (goal-bounded: cut ≤0.75%/wk BW, bulk ≤0.5%, defaults 0.5/0.3, `rate_clamped` flag; `kcal_floored` to 1200♀/1500♂/1350 floor) + macros (protein 1.6 g/kg default, **never below the 1.2 safety floor** → `protein_floored`; fat 0.8 g/kg floor; carbs absorb remainder — every number recomputes to kcal within rounding) + per-day-type targets when carb-cycling (**Σ dayType.kcal×count == 7×primary kcal**, protein held constant) + IF eating window (widened to ≥8h unless `allowShortEatingWindow`, `if_window_widened` flag); absurd-input guards (age<16 → `rejected: age_below_minimum`; a malformed carb-cycle config falls back to standard). `compileConstraints(intake, style?)` → the non-allergen-filter half of pipeline step 0 (allergens passed through **verbatim** for P4.2's canonical `packages/ai` `filterSafeFoods` — the engine stays zero-AI-import, allergen net not duplicated; diet pattern, notes, meals/day with meal-time fallback, cuisine weights + dislikes from style). `parseIntake(rawIntake, rawHealthFlags)` Zod-parses the untyped `clients.intake` Json + `health_flags.allergies` into a typed `IntakeInput`, missing biometrics → structured issues, never a throw. **`plans` table** (`20260723120000_plans.sql`): version/status(draft|approved|superseded|archived)/protocol/day_types/content(P4.2)/rationale/source(reuses `plan_trigger`)/based_on_plan_id(P4.4)/approved_at·by; RLS — staff read whole org incl. drafts, **clients read only their own APPROVED plan** (drafts invisible), writes service-role only (grant SELECT only → no write-policy hole). Verified: `supabase db reset` clean · **pgTAP 191** (+8: plans staff-org/client-own-approved reads, draft invisibility, cross-client isolation, write-denial) · typecheck 6/6 · lint (0 errors) · **Playwright 220** (+25 engine fixtures: BMR/activity/tdee, cut/bulk/recomp, floors, protein floor, carb-cycle weekly-sum, IF window, guards, constraints, parse). Commit: "feat(nutrition-engine): coded calculation + constraint compiler".
